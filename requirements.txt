# Загрузчик, HTTP-слой и control plane. docling здесь нет: он живёт в своём
# контейнере, общение с ним идёт по HTTP, а веса и torch лежат в его образе.
#
# Здесь ТОЛЬКО то, что нужно всем ролям. torch и FlagEmbedding вынесены
# в requirements-embed.txt: их держит процесс API, а воркеры RQ модель
# не загружают — за векторами они ходят в /embed.
#
# Для локального запуска нужны оба файла:
#   pip install torch --index-url https://download.pytorch.org/whl/cu128
#   pip install -r requirements.txt -r requirements-embed.txt

# --- HTTP-клиенты ---
httpx

# --- OpenSearch ---
opensearch-py[async]

# --- HTTP-слой ---
fastapi
uvicorn[standard]
pydantic
pydantic-settings
python-multipart

# --- control plane ---
sqlalchemy[asyncio]>=2.0
asyncpg
psycopg[binary]     # синхронный драйвер для alembic
alembic
redis
rq

# --- объектное хранилище (SeaweedFS через S3-шлюз) ---
boto3

# --- переупаковка иконок: отбрасывает всё, что не пиксели ---
pillow